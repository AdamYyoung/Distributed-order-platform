server:
  port: 8085

spring:
  application:
    name: notification-service
  data:
    redis:
      host: localhost
      port: 6379

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
